Make the failing unit tests pass.

The project in `examples/broken_math` has a bug in `math_utils.py` that makes
the tests in `test_math.py` fail. Fix the source so every test passes.

Run the tests with:  python -m pytest -q
